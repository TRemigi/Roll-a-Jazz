import React from 'react';

import { useQuery } from '@apollo/react-hooks';

const Home = () => {

  return (
    <main>
      <div>
        <div> Project stuff goes here</div>
      </div>
    </main>
  );
};

export default Home;
